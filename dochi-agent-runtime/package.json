{
  "name": "dochi-agent-runtime",
  "version": "0.1.0",
  "description": "Dochi Agent Runtime Sidecar - Claude Agent SDK bridge",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "test": "tsc -p tsconfig.test.json && node --test dist-test/tests/",
    "test:typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "private": true,
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.0.0"
  }
}
